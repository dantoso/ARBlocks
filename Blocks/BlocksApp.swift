import SwiftUI

/// Here is where the app begins
@main
struct BlocksApp: App {
    var body: some Scene {
        WindowGroup {
			// calling the AppStart View, to show the App's initial interface
			AppStartView()
        }
    }
}
